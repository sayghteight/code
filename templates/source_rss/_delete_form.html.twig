<form method="post" action="{{ path('app_source_r_s_s_delete', {'id': source_r_s_s.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ source_r_s_s.id) }}">
    <button class="btn">Delete</button>
</form>
